// Kept as a compatibility shim for older imports. The previous implementation
// bundled a full WebGL scene even though this component rendered nothing.
export function ParticleBackground() {
  return null;
}
