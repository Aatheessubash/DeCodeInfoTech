# Server-only settings. Copy to .env.local; never commit real credentials.
SMTP_SERVICE=gmail
SMTP_USER=
SMTP_PASS=
# For a custom SMTP server, set SMTP_SERVICE to an empty value.
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
MAIL_FROM=
ADMIN_RECEIVER_EMAIL=
ADMIN_USERNAME=
ADMIN_PASSWORD=
